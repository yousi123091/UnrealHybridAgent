# UHA v0.2 路线

优先完成当前安全验收门槛，而不是扩展功能。

1. 用户参与新版 worker 的物理鼠标 mid-drag、键盘 held-key、中途 takeover；测量最后一次自动事件与接管时间，确认终态不能自动重开。旧 Sept21 动作间人工记录保留，不自动继承到新实现。
2. 取消与释放压力验证：文件锁/磁盘错误、hook timeout、双进程同时死亡、长时间调度停顿；降低与测量检查/注入窗口。验证 unhook API 实际返回及 OS hook 超时后行为。
3. Provider 的远端在途任务标识与协作取消、受控子进程树、重启后的只读重建；不确定写禁止自动重放。
4. 基于真实碰撞与表面的复杂 mesh support、多层楼板、斜面、空洞、悬挂对象语义；缺证据保持 UNKNOWN。重复身份与对象换名的稳定 ID 恢复。
5. UE 世界切换采用引擎安全调度/API 协议，跨关卡生命周期单独验收；不得解除本轮保护后继续在 tick RPC 直接替换世界。
6. 干净机器安装验收、受支持 UE 版本矩阵、长期 CPU/内存/子进程泄漏与 p95；提高异常审计和日志脱敏。
7. 发布前清理公开候选树与完整历史秘密审计，明确许可证和第三方依赖；经用户明确指令才创建/推送远程仓库。

## UAH 的定位（重要，避免误读）

UAH 作为**同仓库中长期 first-class 子系统**保留并继续开发，源码不移除。

但是——**在 `v0.1.0-rc.2` 当前公开 Release 范围中，UAH 的 HUD 与 standalone
entry points 暂不启用**；legacy `ControlOverlay` 同样暂停，且无法通过旧配置重新启用。
发布门禁会阻止它们启动（见 `tests/test_release_scope.py`）。

因此：

- 不要按历史报告里的 HUD 启动命令验收本版本；
- `uah/` 下的源码是**下一版的工作基础**，不是本 Release 已交付的功能；
- 当前权威状态见 [`docs/STATUS.md`](docs/STATUS.md)。

不扩展手机、账号、云同步；界面仅修复实际使用问题。ControlOverlay 没有迁移必要前保持现状。
